package %PACKAGE%;

import io.anuke.ucore.modules.ModuleCore;

public class %MAIN_CLASS% extends ModuleCore {
	
	@Override
	public void init(){
		module(Vars.control = new Control());
		module(Vars.ui = new UI());
	}
	
}
